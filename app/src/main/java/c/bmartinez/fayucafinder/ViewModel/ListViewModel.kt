package c.bmartinez.fayucafinder.ViewModel

import android.app.Application
import androidx.annotation.NonNull
import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel
import c.bmartinez.fayucafinder.Model.Database.fayucaDao

class ListViewModel : ViewModel() {

    /*private lateinit var firebaseInit: FirebaseInit
    private lateinit var vehicles: MutableLiveData<fayucaDao>

    fun ListViewModel(@NonNull application: Application){
        firebaseInit = FireBaseInit()
    }

    private val _text = MutableLiveData<String>().apply {
        value = "This is dashboard Fragment"
    }
    val text: LiveData<String> = _text*/
}